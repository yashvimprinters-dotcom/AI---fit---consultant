# FITORA V31 – Android App Icon / PWA Install Fix

Adds a proper web app manifest, branded 192x192 and 512x512 FITORA icons, favicon, Apple touch icon, and a root-scoped service worker. After deployment, remove the old FITORA home-screen shortcut and install FITORA again from Chrome so Android refreshes the icon/manifest.
