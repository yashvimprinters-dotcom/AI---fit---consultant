# FITORA v22 — logo reliability fix

The FITORA logo is now embedded directly in `static/index.html`, so the header does not depend on a separately uploaded image path. The original PNG is also retained under `static/assets/fitora-logo.png` as a backup.

Upload/replace `app.py`, `requirements.txt`, and `static/index.html` from this package. Keep the rest of the project files.
